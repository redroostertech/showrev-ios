# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShowRev' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Comment the following if you want to only handle changes and code that exists in the master repo
  pod 'RRTech-Starter-App-Template', :git => 'https://github.com/redroostertech/RRTech-Starter-App-Template.git'

  # Comment the following if you want to handle continuous changes to a local repo of the forcemaps-ios SDK.
  # Will be removed for production
  # pod 'RRTech-Starter-App-Template', :path => '~/Desktop/RRTech-Starter-App-Template'

  # Pods for ShowRev

  target 'ShowRevTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShowRevUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
